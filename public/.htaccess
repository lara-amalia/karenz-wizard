<IfModule mod_headers.c>

  # HTML
  # Cache: 0s (always revalidate)
  # Reason: entry point (.html files) changes on every deploy
  <FilesMatch "\.(html)$">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </FilesMatch>

  # PDF
  # Cache: 0s (always revalidate)
  # Reason: static filename, but PDF is regenerated on deploy if content changed
  <FilesMatch "\.(pdf)$">
    Header set Cache-Control "public, max-age=0, must-revalidate"
  </FilesMatch>

  # JS / CSS (hashed)
  # Cache: 24h
  # Reason: good performance, low failure risk (Astro emits
  # hashed file names under /_astro/ on every build / deploy)
  <FilesMatch "\.(js|css)$">
    Header set Cache-Control "public, max-age=86400"
  </FilesMatch>

  # Images / fonts
  # Cache: 7d
  # Reason: rarely change, safe moderate TTL
  <FilesMatch "\.(png|jpg|jpeg|svg|webp|woff2)$">
    Header set Cache-Control "public, max-age=604800"
  </FilesMatch>

</IfModule>